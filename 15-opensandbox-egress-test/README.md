# OpenSandbox egress restriction test (mirrored from opensandbox-lab)

Test plan + results for Alibaba OpenSandbox's `egress` sidecar (FQDN-based
network egress control) on `cluster1-134`. Source of truth is the separate
`opensandbox-lab` repo (local-only, not yet pushed); this copy is mirrored
here temporarily so results have a pushed remote home.

See `TEST_PLAN.md` for the 9-case plan and `TEST_RESULTS.md` for the
executed results (all cases run with real request/log evidence).
